'use strict'

/* const scalar = {
  id: '/obj/filter/scalar',
  title: 'Scalar',
  oneOf: [
    {
      type: 'string',
    },
    {
      type: 'number',
    },
  ],
}

const expression = {
  id: '/obj/filter/expression',
  title: 'Expression',
  oneOf: [
    { type: 'null' },
    { $ref: scalar.id },
    {
      type: 'object',
      properties: {
        gt: { $ref: scalar.id },
        gte: { $ref: scalar.id },
        lt: { $ref: scalar.id },
        lte: { $ref: scalar.id },
      },
      additionalProperties: false,
    },
  ],
}

const expresssionList = {
  id: '/obj/filter/expresssionList',
  title: 'Expression List',
  type: 'array',
  items: { $ref: expression.id },
}

const filterCompound = {
  id: '/obj/filter/filterCompound',
  title: 'Filter Compound',
  type: 'object',
  properties: {
    or: { $ref: expresssionList.id },
    and: { $ref: expresssionList.id },
  },
}

const filter = {
  type: 'object',
  properties: {
    or: {

    },
    and: {

    },
  },
  additionalProperties: false,
}*/

// chybi not
